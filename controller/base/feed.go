package base

import (
	"context"
	"github.com/cloudwego/hertz/pkg/app"
)

func FeedController(c context.Context, ctx *app.RequestContext) { //接受视频处理
	//last_name := ctx.Query("last_name")
	//token := ctx.Query("token")
}
